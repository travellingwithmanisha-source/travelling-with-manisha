export const metadata = { title: "About" };

export default function AboutPage() {
  return (
    <div className="container max-w-2xl py-16">
      <h1 className="mb-4 font-display text-3xl font-semibold">About us</h1>
      <p className="text-muted-foreground">
        Placeholder copy — replace with the real story once it's written.
        Travelling with Manisha connects travelers with homestay owners and
        curated tour operators, with transparent pricing and an itinerary
        you can see before you commit.
      </p>
    </div>
  );
}
